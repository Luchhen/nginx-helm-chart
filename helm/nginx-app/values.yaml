replicaCount: 1

image:
  repository: 10.0.2.15:8080/library/nginx-app
  tag: "c6249ff2fb05e23abd30c1f7b01c4c3a90c6c70e"
  pullPolicy: Always

imagePullSecrets:
  - name: harbor-secret

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true

# Add to your existing values.yaml
signoz:
  enabled: true
  endpoint: "192.168.1.128:4317"   # e.g. 10.0.0.5:4317
  serviceName: nginx-argocd-app
  environment: production
